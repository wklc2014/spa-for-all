import m from 'mithril';

const Hello = {
  oninit: (vnode) => {
    // console.log('Hello component oninit');
  },
  view: function (vnode) {
    return (
      <div>
        <h3>Help</h3>
        <div>

        </div>
      </div>
    );
  }
};

export default Hello;
