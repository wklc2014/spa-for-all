import React from 'react';
import propTypes from 'prop-types';

const Index = () => {
    return (
        <div>
            this is Index page
        </div>
    );
};

Index.propTypes = {
};

export default Index;
