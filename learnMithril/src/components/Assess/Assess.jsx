import m from 'mithril';

const Assess = {
    oninit: (vnode) => {

    },
    view: function (vnode) {
        return (
            <div>My First Mithril component</div>
        );
    }
};

export default Assess;
